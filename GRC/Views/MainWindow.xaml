<Window
    x:Class="GRC.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:GRC.Helpers"
    xmlns:views="clr-namespace:GRC.Views"
    xmlns:vm="clr-namespace:GRC.ViewModels"
    Title="GenAI Roleplay Chat"
    Width="600"
    Height="850"
    helpers:WindowTitleBarBehavior.EnableDarkTitleBar="True"
    Background="#343541"
    Icon="/Resources/GRC.ico"
    WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:SessionListViewModel}">
            <views:SessionListView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ChatViewModel}">
            <views:ChatView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentPage}" />
    </Grid>
</Window>